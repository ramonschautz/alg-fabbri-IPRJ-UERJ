// rfabbri 2025-09-25
#ifndef SORT_CHAR_H
#define SORT_CHAR_H

/*
  A função foi nomeada 'sort_char' em vez de 'sort-char' porque hífens não são permitidos em nomes de funções em C.
  A função também foi generalizada para aceitar o tamanho do array como um argumento 'n', em vez de um tamanho fixo de 1000.
*/
void
sort_char(char *v, int n);

#endif // SORT_CHAR_H
